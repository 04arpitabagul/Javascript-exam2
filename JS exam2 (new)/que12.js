// ### 12. **Array Methods (push and pop)**

// - Task: Add an element to the array using `push()` and remove one using `pop()`.
// - Rubric: 3 points for correct use of `push()`, 2 points for `pop()`.

let arr1 = ["Love" , "Hate" , "Peace" ];
arr1.push("fear");
console.log(arr1);

arr1.pop();
console.log(arr1);
